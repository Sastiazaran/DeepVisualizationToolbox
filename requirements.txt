# Runtime dependencies. Kept in sync with [project.dependencies] in pyproject.toml.
# Prefer `pip install -e .` (or `pip install -e ".[dev]"`) which reads pyproject.toml.
tensorflow>=2.16
keras>=3.0
numpy>=1.26
scipy>=1.11
matplotlib>=3.8
pillow>=10.0
opencv-python>=4.9
scikit-image>=0.22
PyQt6>=6.6
